# Quickshell development

Before you run or test Quickshell, read `docs/README.md` and `docs/development/quickshell-dev.md`.

Use `scripts/quickshell-dev` for this source tree. Do not run `quickshell --path` unless the user asks for an isolated test.
