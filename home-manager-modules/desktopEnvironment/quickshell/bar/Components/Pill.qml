import qs
import Quickshell.Widgets

// Inner pill: surface1 background for individual items in the right section.
WrapperRectangle {
    color:  Theme.alpha(Theme.surface1, Theme.bgAlpha)
    radius: Theme.innerRadius
}
