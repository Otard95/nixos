import qs
import Quickshell.Widgets

// Outer pill: base background for workspaces, active window, and the
// right-section group container.
WrapperRectangle {
    color:  Theme.alpha(Theme.base, Theme.bgAlpha)
    radius: Theme.outerRadius
}
